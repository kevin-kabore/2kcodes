# Database Configuration
# PostgreSQL connection string - Required for full features
# For local development: postgresql://username:password@localhost:5432/2kcodes
# For production: Use your cloud database URL
DATABASE_URL="postgresql://username:password@localhost:5432/2kcodes"

# Authentication Configuration
# Required for NextAuth.js authentication features
# NEXTAUTH_URL should match your deployment URL
NEXTAUTH_URL="http://localhost:3000"

# Generate a secure secret with: openssl rand -base64 32
NEXTAUTH_SECRET="your-secret-key-here"

# Web3 Configuration (Optional)
# Get your environment ID from https://app.dynamic.xyz
DYNAMIC_ENVIRONMENT_ID="your-dynamic-environment-id"

# Deployment Configuration
# Set to "true" for static export (GitHub Pages)
# Set to "false" for server deployment (Vercel, etc)
NEXT_PUBLIC_STATIC_EXPORT="false"

# Development vs Production
# This is automatically set by Next.js
# NODE_ENV="development"