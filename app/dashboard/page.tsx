import { DashboardContent } from './dashboard-content'

export default function DashboardPage() {
  // Server component - no auth check needed here since Dynamic handles it client-side

  return <DashboardContent />
}